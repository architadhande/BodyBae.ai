# Core dependencies (pinned to stable versions)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Lightweight RAG alternatives
langchain==0.0.350
openai==0.28.1
sentence-transformers==2.2.2
PyPDF2==3.0.1

# Compatible versions for Python 3.9-3.11 (Render's default)
scikit-learn==1.0.2  # Lighter version that works universally
python-dotenv==1.0.0
numpy==1.24.3  # Widely compatible version

# Explicitly include build tools
setuptools==68.2.2
wheel==0.41.3
